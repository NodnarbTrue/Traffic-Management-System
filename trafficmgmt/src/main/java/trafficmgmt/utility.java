package trafficmgmt;

public class utility {
    public enum timerlengthinformation {Direction_LENGTH, YELLOW_LIGHT_LENGTH, LEFT_TURN_LENGTH, CROSSWALK_COUTDOWN_LENGTH}
    public enum entitytype {TRAFFICLIGHTS, CROSSWALKS}
    public enum direction {NORTH, EAST, SOUTH, WEST, DIRECTION_ONE, DIRECTION_TWO}
    public enum lightState {GREEN, YELLOW, RED, LEFT_TURN, LEFT_TURN_YELLOW}
    public enum crosswalkState {STOP, WALK, COUNTDOWN}
}
